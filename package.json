{
  "name": "geoweb3-dapp-restored-no-esri-production",
  "version": "4.0.0",
  "private": true,
  "description": "GeoWeb3 DApp restored feature set with independent non-Esri metadata submission, OSM rewards, DAO verification, contribution faucet and production scaffolding.",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start:backend": "cd backend && npm start"
  }
}
